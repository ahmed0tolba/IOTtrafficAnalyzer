"""IOTNetworkModel.ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/19ocznePnGk18oG9jBIXUyAdBaxrZ3aeK

Convert kali to access point and log a set of IoT devices into it.
Capture the traffic using Wireshark.
Extract traffic features for each IoT device and store for future ML/DL models to analyse.
by Ahmed Tolba
"""

python 3.7.9

// make sure repositary is public
git clone https://github.com/ahmed0tolba/IOTtrafficAnalyzer.git --branch master
sudo apt-get update

sudo apt-get install python3-pip -y
sudo apt-get install tshark -y

cd IOTtrafficAnalyzer/
mkdir uploadedFiles
pip install -r requirements

#sudo to be able to run on port 80
pip install flask flask_sqlalchemy pyshark xlsxwriter pandas openpyxl Flask-AutoIndex tqdm

sudo python3 application.py

nohup python3 application.py

in C:\\Users\\ahmed\\AppData\\Roaming\\Python\\Python37\\site-packages\\pyshark\\capture\\capture.py
replace 
            # current_eventloop = asyncio.get_event_loop_policy().get_event_loop()
            
            with
            
            current_eventloop = asyncio.new_event_loop() 
            asyncio.set_event_loop(current_eventloop) 